function parse(rows){
	console.log('num of row:', rows.length);
	console.log('row[0]:', rows[0]);
	// Build rows updateToDrive Lunch Money
	// headers	[date, memA, memB, memC, memD,..]
	// rows		[date, 17, 18, 19, 33,..]
	let record = [];

	return record;
}

module.exports = parse;